const GENDER = {
  MALE: 'male',
  FEMALE: 'female',
  OTHER: 'other',
}

const ROLE = {
  ADMIN: 'admin',
  USER: 'user',
}

const EXPIRES_IN = 14 * 24 * 60 * 60 * 1000

module.exports = {
  GENDER,
  ROLE,
  EXPIRES_IN,
}